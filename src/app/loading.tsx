import { FullPageLoader } from "@/shared/components/ui/states";

/** Fallback de carga a nivel de aplicación mientras se resuelve el árbol. */
export default function Loading() {
  return <FullPageLoader />;
}
